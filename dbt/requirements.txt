# Instale num virtualenv Python 3.9+ :
#   python -m venv .venv && . .venv/bin/activate   (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt
# Testado ponta a ponta com dbt-core 1.12.4 / dbt-bigquery 1.12.0 (Python 3.13).
dbt-bigquery~=1.12.0
