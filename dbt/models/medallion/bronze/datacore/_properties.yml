version: 2

# _properties.yml — sistema "DataCore" (datacore), camada Bronze.
# TODOS os modelos deste sistema. GERADO por server/dbtCodegen.ts a partir de
# dbt/_generated_bronze.json — não editar à mão.
models:
  - name: bronze_datacore_dbt_execucoes
    description: "Bronze gerado — tabela dbt_execucoes (sem PK; sem deduplicação)."
  - name: bronze_datacore_dbt_modelos
    description: "Bronze gerado — tabela dbt_modelos (sem PK; sem deduplicação)."
  - name: bronze_datacore_dbt_ultima_execucao
    description: "Bronze gerado — tabela dbt_ultima_execucao (sem PK; sem deduplicação)."
  - name: bronze_datacore_destinos
    description: "Bronze gerado — tabela destinos (sem PK; sem deduplicação)."
  - name: bronze_datacore_empresas
    description: "Bronze gerado — tabela empresas (sem PK; sem deduplicação)."
  - name: bronze_datacore_integracoes
    description: "Bronze gerado — tabela integracoes (sem PK; sem deduplicação)."
  - name: bronze_datacore_origens
    description: "Bronze gerado — tabela origens (sem PK; sem deduplicação)."
  - name: bronze_datacore_tabelas_carregadas
    description: "Bronze gerado — tabela tabelas_carregadas (sem PK; sem deduplicação)."
  - name: bronze_datacore_usuarios
    description: "Bronze gerado — tabela usuarios (sem PK; sem deduplicação)."
